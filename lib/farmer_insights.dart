import 'package:flutter/material.dart';

class FarmerInsightsPage extends StatelessWidget {
  const FarmerInsightsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Insights Page',
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}