import 'package:flutter/material.dart';

class PartnerHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [Text("Partner Home")]));
  }
}
